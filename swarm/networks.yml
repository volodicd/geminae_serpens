version: '3.8'

networks:
  serpens-net:
    driver: overlay
    attachable: true
    driver_opts:
      encrypted: "true"
    ipam:
      config:
        - subnet: 10.0.9.0/24
  
  serpens-internal:
    driver: overlay
    internal: true
    ipam:
      config:
        - subnet: 10.0.10.0/24
